# TestFlight 1.2 (2026090603)

The user explicitly authorized uploading the current changes to TestFlight. The requested version is 1.2. All four app/widget Debug/Release settings use marketing version 1.2 and build 2026090603.

## Scope

Includes required sport/city onboarding and session routing, removal of the map visibility control from the email/Apple sign-in card, and temporary removal of the activity feed entry and preview bypass. Existing native changes since the prior TestFlight remain included. The main onboarding map control, profile setting, native preference handling, and backend contract remain unchanged during release preparation.

No native product changes were made during release preparation. The source snapshot differs from the validated hide-feed snapshot only in the exact requested auth-card control/binding removal and version metadata. The previous task's simulator and physical-device feed smoke checks therefore cover unchanged navigation/feed code; this release also rebuilds the combined snapshot.

## Frozen source and artifacts

- Snapshot: `build/TestFlight/source-1.2-2026090603/`, all 95 native source/resource/project/test files, including untracked files and assets.
- Full SHA-256 manifest: `build/TestFlight/release-2026090603-manifest.json`.
- Excluded build products, DerivedData, xcuserdata, .DS_Store, Local.xcconfig, and xcuserstate. Local developer overrides are not part of the release.
- Archive: `build/TestFlight/TennisSearchIOS-1.2-2026090603.xcarchive`.
- Archive log: `build/TestFlight/archive-1.2-2026090603.log`.
- Simulator log: `build/TestFlight/simulator-1.2-2026090603.log`.
- App Store Connect upload configuration: `build/TestFlight/UploadOptions-1.2-2026090603.plist` (automatic signing, team M2ZZ39HQZ5; build auto-renumbering disabled).
- Build configuration is explicitly HTTPS sportsearch.shop, mock data NO, debug server trust NO, APNS production.

## Changes since 2026090602

The preceding isolated snapshot is unavailable; the prior full manifest establishes the exact changed-file inventory. No prior sources have been reconstructed or substituted.

Modified:
- `ios/TennisSearchIOS.xcodeproj/project.pbxproj`
- `ios/TennisSearchIOS/App/AppModel.swift`
- `ios/TennisSearchIOS/Core/AppModels.swift`
- `ios/TennisSearchIOS/Services/APIClient.swift`
- `ios/TennisSearchIOS/Services/MockRepository.swift`
- `ios/TennisSearchIOS/Services/TennisRepository.swift`
- `ios/TennisSearchIOS/Views/AuthView.swift`
- `ios/TennisSearchIOS/Views/ContentView.swift`
- `ios/TennisSearchIOS/Views/CourtsView.swift`
- `ios/TennisSearchIOS/Views/DiscoverPlayersMap.swift`
- `ios/TennisSearchIOS/Views/DiscoverView.swift`
- `ios/TennisSearchIOS/Views/EmptyDeckView.swift`
- `ios/TennisSearchIOS/Views/ProfileView.swift`
- `ios/TennisSearchIOS/Views/SportsActivityFeedView.swift`
- `ios/TennisSearchIOS/Views/UIComponents.swift`

Added:
- `ios/Tests/PlayerAutoAdvanceTests.swift`
- `ios/Tests/RequiredOnboardingTests.swift`
- `ios/Tests/run-player-auto-advance-tests.sh`
- `ios/Tests/run-required-onboarding-tests.sh`

No source or resource files were removed.

## Verification

- Release archive: **PASS**, xcodebuild exit 0 / `ARCHIVE SUCCEEDED`.
- Frozen Debug simulator build: **PASS**, xcodebuild exit 0 / `BUILD SUCCEEDED`.
- Focused actual-source tests: **36 onboarding + 39 player auto-advance assertions PASS**. Logs `build/TestFlight/required-onboarding-tests-2026090603.log` and `build/TestFlight/player-auto-advance-tests-2026090603.log`. The final 1.2 freeze changes only version metadata from the tested source.
- Frozen source integrity after archive/export: **95/95 SHA-256 hashes match**.
- Actual app and widget identifiers are `shop.sportsearch.app` and `shop.sportsearch.app.UpcomingGamesWidget`; both contain **1.2 (2026090603)** and HTTPS sportsearch.shop. Main app mock and debug trust flags are NO; APNS configuration is production.
- Strict deep archive signatures: **PASS**. The automatic archive is development-signed; the verified local App Store export performs distribution signing.
- Local App Store export: **PASS**, `build/TestFlight/local-export-1.2-2026090603.log` / `EXPORT SUCCEEDED`. Separate local-only options use `destination=export`; this step did not upload.
- Verified IPA: `build/TestFlight/export-1.2-2026090603-local/Sportia.ipa`, SHA-256 `010ce09e936adc74f15d95f263f388bf39a012cdff504c90197830fbc319049f`.
- Exported app and widget: **Apple Distribution**, team M2ZZ39HQZ5, strict deep signature PASS, App Store provisioning profiles, `get-task-allow=false`; main signed `aps-environment=production`. Both binary UUIDs match the archive.
- Reports: `build/TestFlight/verification-1.2-2026090603.json` and `build/TestFlight/distribution-verification-1.2-2026090603.json`.
- Independent release QA: **PASS**, including the actual distribution IPA metadata, production entitlement, signatures, and source freeze.

Existing ProfileView compiler warnings remain: deprecated AVAsset.duration and capture of AVAssetExportSession in a Sendable closure. There were no compiler errors. The feed entry is deliberately hidden; the earlier device hang is not claimed repaired. Backend compatibility review passed; this native release requires no backend deployment.

## Upload result

**Upload PASS at 2026-09-06 19:32:41 Moscow time.** `build/TestFlight/upload-1.2-2026090603.log` reports `Uploaded package is processing`, `Upload succeeded`, and `EXPORT SUCCEEDED`; xcodebuild exited with code 0. Apple accepted **1.2 (2026090603)**. Availability to TestFlight tester groups after processing is not independently confirmed. No public App Store release was submitted.

Backend was not deployed: its existing city/sport validation is compatible with this native fix, and production health/readiness remained successful. Web fixes remain local. The separate earlier map preference persistence feature is not yet present in the production schema; optional request fields are ignored and native responses decode safely. Audit evidence: `.artifacts/backend-audit/20260906-onboarding-compatibility.md`.

An earlier candidate archive was interrupted and was not uploaded; discarded artifacts are explicitly prefixed `discarded-1.1.1-`.
