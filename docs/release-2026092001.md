# Security fixes — TestFlight 1.2.1 (2026092001)

The user manually tested the audited fixes, confirmed that they work, and explicitly authorized updating the backend and delivering the iOS build through TestFlight. No public App Store release submission is included.

## Scope and source

The accepted scope is described in [the audit report](ios-security-audit-2026-09-20.md). Release responsibilities: root freezes sources, builds/signs/uploads and coordinates activation; mobile specialist checks existing distribution/Apple metadata; backend specialist prepares a selective production overlay; independent QA checks source and distribution artifacts.

The native snapshot is `build/TestFlight/source-1.2.1-2026092001`, with 116 SHA-256 entries in `build/TestFlight/release-2026092001-manifest.json`. It contains the audited iOS app, widget, shared Keychain store, and regression scripts. All four app/widget build numbers advance from 2026091904 to 2026092001; marketing version remains 1.2.1. Local developer overrides and generated outputs are excluded. The latest locally recorded accepted TestFlight baseline is 1.2.1 (2026091904).

Concurrent regular-play and notification-campaign changes are outside this release. Backend runtime changes are restricted to auth request/verify/logout routes, APNs registration, `src/lib/auth.ts`, the auth i18n files, and `src/server/auth-rate-limit.ts`. Production source is the baseline for a selective overlay; do not deploy the dirty working tree wholesale. No schema/dependency migration is included.

## Acceptance

- Archive/export app and widget report 1.2.1 (2026092001), expected bundle IDs and team M2ZZ39HQZ5.
- Actual distribution bundles use HTTPS sportsearch.shop, mock data/debug trust disabled, production APNs, correct shared Keychain entitlement, matching App Store profiles/signatures and dSYMs.
- Frozen hashes remain unchanged through upload. Export options explicitly disable Apple's automatic build renumbering.
- Apple accepts upload; processing/TestFlight availability is checked when the authenticated Apple account permits it.
- Backend activation preserves DB/Redis/environment and previous immutable image; verify DB readiness and Redis connectivity independently of static health.

## Validation status

The audited code passed lint, 683 server tests, server build/Prisma generation, native regression scripts, Debug and Release simulator builds, independent QA, and the user's manual test.

The signed device archive and App Store export succeeded. Independent QA verified all 116 frozen source hashes, both distribution signatures and provisioning profiles, production APNs, shared Keychain group `M2ZZ39HQZ5.shop.sportsearch.session`, App Group, HTTPS production configuration, disabled debug trust/mock data, and matching binary/dSYM UUIDs. Exported IPA SHA-256: `2a536e297a6134ed04f60f600886f7cdde578fd0d4b51d12c2f5be92b052ddb9`. Evidence: `build/TestFlight/independent-distribution-verification-2026092001.json`.

Apple accepted **1.2.1 (2026092001)** on 2026-09-20 at 23:31 Moscow time. Xcode reported `Uploaded package is processing`, `Upload succeeded`, and `EXPORT SUCCEEDED`; the upload process exited successfully. Evidence: `build/TestFlight/upload-2026092001.log`. [TestFlight in App Store Connect](https://appstoreconnect.apple.com/apps/6768862885/testflight). Final processing state and tester availability are not independently confirmed: the optional read-only App Store Connect API request returned 401, and the browser has no authenticated session. Upload used Xcode's existing Apple account successfully. No public App Store submission was made.

The isolated backend candidate separately passed lint, 683 tests in 91 files, and production build with Prisma generation. Its 459-file source manifest contains exactly eight backend runtime fixes, seven test files, one security document, and release image metadata on top of the verified deployed baseline. No environment files are in the archive or Docker build context.

The remote image built successfully: `sha256:a2826bb2f6123352a4efd4765dce5af1a32b049a5104347713945385a262590d`; all 357 runtime source hashes matched. The final staging guard correctly stopped because the running app container was independently recreated during the build. It still used the previous immutable image `sha256:c8c8b40cb41367c147a5bce711c1b54b2c64bc28caaf5b09da0d5cef0ec2c899`; DB/Redis container IDs were unchanged. The concurrent change affected only the runtime setting `PENDING_ACTION_REMINDERS_ENABLED`, with no changed build arguments. The latest environment was copied server-side into the staged release, preserving that setting without exposing its value.

A fresh baseline verification at 20:50 UTC passed: 356 original and 357 candidate runtime file hashes, expected runtime environment, unchanged DB/Redis and mounts, no competing deployment processes, internal/public health and DB readiness 200, unauthenticated `/me` 401, Redis PING and nonmutating EVAL. The original failed staging exit is retained honestly; refreshed evidence is `.artifacts/ios-auth-release-20260920/refreshed-stage-evidence.json`.

After independent preactivation QA passed, root activated `/opt/tennis-search/releases/20260920-ios-auth-security`. The detached activation wrapper exited **0** and confirmed the expected new image `sha256:a2826bb2f6123352a4efd4765dce5af1a32b049a5104347713945385a262590d`. Only the app container was recreated; environment, mounts, DB/Redis containers and current reminder setting were preserved. Internal and public `/health` and `/ready` returned 200, unauthenticated `/me` returned 401, and Redis PING returned PONG. An independent external `/ready` request after activation also returned 200. No migration, seed, test-account creation or production notification was performed. The prior immutable image remains available with the verified rollback script.

Rollout evidence: `.artifacts/ios-auth-release-20260920/activation-evidence.json`; independent preparation review: `.artifacts/ios-auth-release-20260920/independent-preactivation-qa.json`. Server logs and exact exit status remain in `/tmp/tennis-ios-auth-20260920/activate.log` and `activate.exit`.

Final independent release QA: **PASS**, recorded in `.artifacts/ios-auth-release-20260920/independent-release-qa.json`. It matched the active image and preserved DB/Redis IDs against the audited candidate and refreshed baseline, confirmed postactivation checks, and rechecked native distribution and Apple upload evidence. No authenticated production smoke test or physical-device distribution test was performed during rollout.

Physical-device runtime behavior of the exported distribution build, shared Keychain migration, and real APNs remain manual verification points; distribution signing and entitlements passed artifact verification. Offline server revocation remains best effort. Existing AVFoundation video-trimming warnings are unchanged.
