# TestFlight 1.2.1 (2026090901)

User explicitly authorized releasing the notification navigation fix. The initial 1.2 candidate was rejected because Apple closed that train after approving 1.2. Marketing version therefore advances to 1.2.1; the unaccepted build remains 2026090901. Rejected attempt evidence is retained in `docs/release-2026090901-rejected-1.2.md` and `build/TestFlight/upload-1.2-2026090901.log`.

## Scope and ownership

Root owns freezing, builds, signing and upload. Independent release_qa verifies source and distribution artifacts. Follow existing TestFlight delivery; no public App Store submission or backend deployment.

Freeze `build/TestFlight/source-1.2.1-2026090901`, 97 files, full hashes in `build/TestFlight/release-2026090901-manifest.json`. Baseline: latest uploaded release 1.2 (2026090604). Unrelated working-tree changes are excluded. Exactly two files differ:

- `ios/TennisSearchIOS/Views/DiscoverView.swift`: explicit notifications presentation state/destination, cancel auto-advance before push, gate source geometry/playback, bell accessibility label/identifier.
- `ios/TennisSearchIOS.xcodeproj/project.pbxproj`: four marketing versions 1.2→1.2.1 and four build numbers 2026090604→2026090901 for app/widget.

Product code exactly matches the first candidate; only marketing-version metadata differs. App/widget production requirements: HTTPS sportsearch.shop, mocks NO, debug trust NO, APNS production, correct Apple Distribution signatures and App Store profiles, archive/dSYM executable UUIDs, unchanged97-file hashes, fresh archive/simulator/test/export success and recorded Apple upload outcome.

No notification-data/seen/badge logic, destination handlers, backend/schema/API contracts or ranking changes. User profile, sport/level, geo/districts, availability, proposal/game-search lifecycle, chat unread, push registration, reliability/no-show and premium rules remain unchanged.

## Validation and risks

Previous candidate passed builds, export and independent distribution checks plus159 frozen native assertions. The notification patch had five successful simulator bell/open/back cycles and group collapse/expand on three mock rows. Actual production hang resolution remains unconfirmed. Physical scrolling/edge-swipe/cancelled pop, 0/1/30-row payloads and slow/offline refresh were not verified. The user has authorized release with these disclosed limitations.

Fresh 1.2.1 actual-source native checks PASS: 39 player auto-advance +84 player-map +36 onboarding=159 assertions (new version-specific logs). Release archive and frozen Debug arm64 simulator builds PASS (exit0). Root archive verifier PASS: both bundles have correct 1.2.1(2026090901) metadata, HTTPS production API, app mocks/debug trust NO and configured APNS production; signatures and97/97 source hashes verified. Local App Store export PASS, distribution IPA `build/TestFlight/export-1.2.1-2026090901-local/Sportia.ipa`; independent distribution audit PASS (`build/TestFlight/distribution-verification-1.2.1-2026090901.json`). App/widget have Apple Distribution signatures, valid App Store profiles with exact app/team identifiers and certificate membership, get-task-allow false, main production APNS, and UUIDs matching archive and dSYM. Local IPA SHA-256: `03742ec1324f966a87fd970b9c092c898e40ff2d85cbc7c7a7e66b19ec586c27`. Web/npm/Prisma checks do not apply to this isolated native release. Existing ProfileView warnings are unchanged.

## Upload result

Apple upload PASS at 2026-09-09 14:07:44 Moscow time: process exit0, `Upload succeeded`, `Uploaded package is processing`, `EXPORT SUCCEEDED`. Log: `build/TestFlight/upload-1.2.1-2026090901.log`. Apple accepted 1.2.1 (2026090901); TestFlight tester-group availability after processing has not been independently confirmed. Frozen source integrity remains97/97 after upload. No public App Store review/release submission or backend deployment.

The first1.2 attempt was rejected with exit70; its log and frozen manifest remain preserved. The final1.2.1 upload succeeded. The account-specific production hang still requires real-device confirmation; successful upload is not evidence of behavioral resolution.
