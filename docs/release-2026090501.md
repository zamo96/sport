# TestFlight 1.1 (2026090501)

User authorized uploading the latest workspace iOS update to TestFlight and deploying Yandex Cloud production only if backend changes were present.

- Included current native work: map-linked player grid and native rich markers, pinned discovery controls, compact Searches modes, empty-deck updates, activity-feed prototype and discovery crash fix. Existing workspace edits were preserved.
- Bumped the application and widget build number from 2026090401 to 2026090501 in all four target configurations; marketing version remains 1.1.
- Release archive PASS: `build/TestFlight/TennisSearchIOS-2026090501.xcarchive`; log `/tmp/archive-2026090501.log`. Deep strict code-signature verification PASS.
- Inspected built main and widget plists: expected bundle identifiers, version 1.1/build 2026090501, production HTTPS API sportsearch.shop. Main app has UseMockData=NO, AllowDebugServerTrust=NO, APNSEnvironment=production. Debug preview shortcuts are excluded from Release. The activity-feed prototype still uses local demonstration posts; it is not a server-backed social feed.
- Upload PASS: `/tmp/upload-2026090501.log` reports `Upload succeeded` and `EXPORT SUCCEEDED` at 2026-09-05 18:47 local time. Apple accepted the package and reported processing. Availability to TestFlight groups after processing has not been independently confirmed: the browser session requires App Store Connect login. No public App Store release was submitted.
- Source manifest: `build/TestFlight/release-2026090501-manifest.json` records base HEAD and hashes of all changed native files included in the archive. Files were not committed or reset.
- Native simulator build and focused 26/26 helper checks passed in the implementation task; final release archive and scoped whitespace checks passed. Web/npm/Prisma checks are not applicable to the native-only release.
- Backend audit: no changes to src, prisma, package manifests, Dockerfile, production compose or deploy configuration relative to HEAD. Production /health returned HTTP 200. No Yandex Cloud deployment or database mutation was needed or performed. Exact running production commit is not exposed by the health endpoint.
- Remaining product verification limits are recorded in the individual native task packets: physical marker-origin pinch, coincident-player picker fixture, large text/narrow devices, activity-feed media import, and Apple Maps base tiles unavailable in the test simulator.
