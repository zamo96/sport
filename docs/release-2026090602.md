# TestFlight 1.1.1 (2026090602)

User authorized uploading the completed photo-report update. Apple rejected the first archive 1.1 (2026090601) because the 1.1 pre-release train is closed after version approval. Incremented all four app/widget configurations to patch version 1.1.1 and fresh build 2026090602, retaining identical product code.

- Includes report thumbnail opening and shared full-image paging in Upcoming, completed personal-visit photo reports in the owner's Profile feed, refresh/error handling and stale-account protection.
- Retains earlier native map, discovery and activity-feed work. The activity-feed prototype remains local demonstration content as documented in the preceding release.
- Prior simulator build and 57 actual-source checks passed. Only version metadata changed after those checks. UI touch/image-layout verification remains limited by unavailable Simulator UI control.
- Source manifest: `build/TestFlight/release-2026090602-manifest.json`, 91 native files. No backend deployment or migration; concurrent analytics/backend/web work is outside this iOS release.
- Archive destination `build/TestFlight/TennisSearchIOS-2026090602.xcarchive`; upload uses the existing automatic-signing App Store Connect export configuration.
- Detected concurrent native edits during the first 1.1.1 archive via the full source manifest. That archive is retained as `TennisSearchIOS-2026090602-concurrent.xcarchive` and will not be uploaded. Created isolated `/tmp/tennis-testflight-2026090602` from all 91 exact manifest hashes, recovering seven changed files from matching historical/QA copies. No shared workspace changes were reverted. The release is rebuilt from this frozen snapshot; authoritative archive log `/tmp/archive-2026090602-isolated.log`.

Final isolated archive verification PASS: `ARCHIVE SUCCEEDED`, all 91 frozen source hashes still match, actual app/widget plists contain 1.1.1 (2026090602), HTTPS sportsearch.shop, main mock NO/debug trust NO/APNS production. Strict deep signature verification PASS. Independent QA confirmed both source freeze and archive configuration/signature.

Upload PASS at **2026-09-06 11:54 Moscow time**. `/tmp/upload-2026090602.log` reports `Uploaded package is processing`, `Upload succeeded` and `EXPORT SUCCEEDED`; uploader exit code 0. Apple accepted **1.1.1 (2026090602)**. Tester-group availability after Apple's processing has not been independently confirmed; no public App Store release was submitted.
