# Выкладка операционной аналитики — 6 сентября 2026

Пользователь авторизовал выкладку изменений аналитики после её реализации и локальной проверки.

## Выпущено

- Адрес: `https://sportsearch.shop/admin/analytics`.
- Release directory: `/opt/tennis-search/releases/20260906-analytics`.
- Docker image: `tennis-search-app:20260906-analytics`.
- Image digest: `sha256:2a6af9579ed79b95dcde7a5ff7263a254091512db48b55319305e61dbc6a35ef`.
- Добавлена аналитика регистрации в первую игру, журнал действий и CSV; подключены серверные события web/iOS и ограниченный сбор web-просмотров. Подробный объём: [task-packet-product-analytics.md](task-packet-product-analytics.md).

## Состав релиза

Релиз собран поверх действующей production-версии `/opt/tennis-search/releases/20260905-photo20`. Перед заменой 23 существующих файлов их содержимое сверено с ожидаемым исходным состоянием Git. Добавлены 6 новых файлов аналитики и 8 тестовых файлов. Все 29 файлов приложения проверены по SHA-256 как в собранном образе, так и в работающем контейнере.

Обновление лимита фотографий из предыдущего релиза сохранено. Другие незакоммиченные iOS/UI/media изменения из рабочего дерева не включались в качестве новых изменений этого релиза. Продакшен-окружение скопировано только внутри сервера, без передачи секретов в локальный пакет. Конфигурация nginx не изменялась, контрольная сумма сохранена.

## Проверки

- На production overlay: `npm run lint` PASS, `npm run test` PASS — 432 теста, 66 файлов.
- Сборка Docker на production-хосте PASS, включая `npm run build`, Prisma generate, lint и проверку типов.
- Подтверждены существующая таблица `UserEvent`, поля, три аналитических индекса и настройка администратора. Новая миграция не выполнялась.
- До переключения новый образ выполнил аналитические запросы на production-БД за 7/30/90 дней и пустую отфильтрованную выгрузку. PASS; только чтение, без тестовых аккаунтов, сессий, событий или игр.
- Переключено только приложение, с проверкой прежнего image digest и сохранением образа отката.
- После переключения `/health` и `/ready` локально и по HTTPS: HTTP 200; приложение healthy.
- Гостевой `/admin/analytics`: HTTP 307 на вход; CSV без авторизации: HTTP 401; приём событий без авторизации: HTTP 401.
- Работающий контейнер соответствует всем 29 ожидаемым SHA-256 исходников. PostgreSQL и Redis продолжили работу без перезапуска. Контрольная сумма nginx не изменилась.
- В логах нового приложения после выкладки не найдены маркеры ошибок Prisma, ошибок записи событий или общих `Error:`.

Авторизованный production-вход и запись действия от реального аккаунта не имитировались. Эти UI/ролевые сценарии проверены локально ранее; сами аналитические SQL-запросы дополнительно проверены на production-БД в новом образе. При проверке перед запуском журнал production содержал 0 событий: данные будут накапливаться по мере использования приложения. Старые действия не восстанавливаются.

## Откат и доказательства

Предыдущий образ сохранён как `tennis-search-app:rollback-20260906-analytics`, digest `sha256:c7bb936f13aa23a51e06951513d92a8a9b1ff708e37bdd49797273bebdae32fd`. Прежние исходники и окружение остаются в `/opt/tennis-search/releases/20260905-photo20`. Файл нового релиза `docker-compose.rollback.yml` указывает на сохранённый образ. Откат касается только приложения; база, Redis и новые события не удаляются.

Локальные manifest, сценарии и логи: `.artifacts/analytics-release/`. Новая версия схемы и iOS/TestFlight-сборка не требовались.
