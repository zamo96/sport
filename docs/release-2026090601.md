# TestFlight 1.1 (2026090601)

User explicitly requested uploading the latest completed iOS photo-report changes to TestFlight.

- Includes tappable game/personal-visit report photos in Upcoming, a shared gallery opening the selected image with all photos and paging, and owner-only completed personal visit reports in the Profile game feed. Preserves the native changes included in the preceding TestFlight build.
- Native implementation passed the simulator build and 57 actual-source checks for feed eligibility, sorting, gallery selection and asynchronous load/account behavior. Visual touch/image-layout checks remain limited by unavailable Simulator UI control; see `docs/task-packet-report-gallery-profile-visits.md`.
- Incremented all four app/widget configuration build numbers from 2026090501 to 2026090601; marketing version remains 1.1.
- Source manifest `build/TestFlight/release-2026090601-manifest.json` records 91 native files. Preserve unrelated concurrent analytics/backend/web work; this release only packages iOS. No backend deployment or database changes requested for this step.
- Production Release configuration uses HTTPS sportsearch.shop, mock data NO, debug server-trust override NO and APNS production. Independent pre-archive check confirmed unchanged tested source before the build-number bump.

- Release archive PASS: `build/TestFlight/TennisSearchIOS-2026090601.xcarchive`; log `/tmp/archive-2026090601.log` reports `ARCHIVE SUCCEEDED`.
- All 91 native manifest files remained unchanged during archive creation. Main app and widget built plists both contain version 1.1/build 2026090601 and the production HTTPS API. Main app mock/trust/APNS settings match the expected production configuration.
- Strict deep signature verification PASS. The completed archive is ready for independent final review and upload.

Upload rejected by Apple validation: version 1.1 is a closed pre-release train and the submitted marketing version must exceed the already approved 1.1. Log: `/tmp/upload-2026090601.log`, `EXPORT FAILED`. This archive was not successfully uploaded. Continue as patch version **1.1.1 (2026090602)**; see `docs/release-2026090602.md`.
