# TestFlight 1.2 (2026090604)

User explicitly authorized uploading the completed player-map changes. Release scope: native schematic district player cards and progressive MapKit grouping, unique-player counts, group zoom and direct card activation, including accessibility. Product design and QA are in `docs/task-packet-player-map-zoom-clusters.md`. No backend, database, web, ranking or consent policy changes are part of this release.

Release sources are frozen in `build/TestFlight/source-1.2-2026090604`, with the complete SHA-256 manifest and delta from 2026090603 in `build/TestFlight/release-2026090604-manifest.json`. Only three existing native product files differ: DiscoverPlayersMap.swift, DiscoverView.swift and MockRepository.swift; the last adds a DEBUG-only stress fixture. Two map test files are added; project changes only advance the four app/widget build numbers. Local developer overrides and build output are excluded.

Root owns freezing, builds, signing, upload and documentation. Independent architecture/QA reviews source scope and final artifacts. Release acceptance: app/widget both 1.2 (2026090604), HTTPS sportsearch.shop, mocks NO, debug server trust NO, valid App Store distribution signatures/profiles with production APNS, unchanged frozen hashes, native checks pass, and Apple upload result recorded. No public App Store submission.

Prior map QA passed 159 assertions and a simulator build, plus runtime group12 → smaller groups → individual cards → correct profile and return. Known manual limitations remain physical tap/pan/pinch/zoom-out, large text, remote photos and unavailable simulator base map tiles. These are not claimed verified by automated release checks.

Validation completed:

- Frozen map, auto-advance and onboarding tests: 84 + 39 + 36 = 159 PASS.
- Release archive and frozen Debug arm64 simulator build: PASS.
- Main app and widget actual archive metadata: 1.2 (2026090604), expected bundle identifiers, HTTPS sportsearch.shop. Main app mocks NO, debug trust NO, APNS configuration production.
- Local App Store export: PASS. Distribution IPA signatures are Apple Distribution, team M2ZZ39HQZ5, strict deep verification PASS. App Store profiles, get-task-allow false, signed main APNS production; both executable UUIDs match the archive.
- Frozen source integrity remains 97/97 matching hashes. Independent source and final artifact QA: PASS, including actual distribution IPA, profile, entitlements and archive UUID checks.
- Web npm/Prisma checks are not applicable to this native-only release. Existing ProfileView compiler warnings remain; no build errors.

Artifacts: `build/TestFlight/TennisSearchIOS-1.2-2026090604.xcarchive`, `export-1.2-2026090604-local/`, build/test logs, `verification-1.2-2026090604.json`, and `distribution-verification-1.2-2026090604.json`.

Apple upload **PASS**: xcodebuild exit 0, `Upload succeeded`, `Uploaded package is processing` and `EXPORT SUCCEEDED`. Apple accepted 1.2 (2026090604); availability to tester groups after processing has not been independently confirmed. No public App Store submission or backend deployment. All 97 frozen source hashes still match after upload. Distribution IPA SHA-256: `0cd73e4a2e2a9782e76de31d2527dd36daa2e67e8f613735cb6d32ce5fb96bfc`.
