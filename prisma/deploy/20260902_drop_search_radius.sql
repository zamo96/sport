BEGIN;

-- Настройка «искать в пределах N км» убрана из продукта: охват задаётся
-- страной, городом и районами, а километры игроку ничего не говорили. В
-- подборе игроков поле и так не участвовало ни в одном фильтре — отсев делала
-- принадлежность к городу, — поэтому сохранённые значения смысла не несут.
ALTER TABLE "User" DROP COLUMN IF EXISTS "searchRadiusKm";

COMMIT;
