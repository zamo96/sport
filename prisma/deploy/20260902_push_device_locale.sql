BEGIN;

-- Язык интерфейса приложения на момент регистрации токена. Нужен для пушей
-- игрокам, у которых нет ни выбранного языка, ни страны в профиле: без него
-- фолбэк отдаёт английский русскоязычным.
ALTER TABLE "PushDevice" ADD COLUMN IF NOT EXISTS "locale" TEXT;

COMMIT;
