// Bump alongside MARKETING_VERSION at each release (see ios/.../project.pbxproj).
// latestVersion drives the dismissible in-app banner; minVersion is raised only
// when older clients must be forced to update (e.g. a breaking API contract change).
export const APP_LATEST_VERSION = "1.2.1";
export const APP_MIN_SUPPORTED_VERSION = "1.0.0";
